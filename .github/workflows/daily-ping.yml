name: Daily Ping Remind Service

on:
  schedule:
    - cron: "0 7 * * *"     # 14:00 giờ Việt Nam
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping remind-service /health
        run: curl -v --max-time 20 https://remind-service.onrender.com/health
